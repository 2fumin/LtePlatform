<h4 class="text-center text-primary">测量报告数统计</h4>
<table class="table table-hover">
    <thead>
    <tr data-bind="foreach: districts">
        <th data-bind="text: $data"></th>
    </tr>
    </thead>
    <tbody data-bind="foreach: mrStats">
    <tr>
        <td data-bind="text: $data.statDate"></td>
        <!-- ko foreach: $data.values -->
        <td data-bind="text: $data"></td>
        <!-- /ko -->
    </tr>
    </tbody>
</table>
<h4 class="text-center text-primary">精确覆盖率统计</h4>
<div style="min-height: 400px" id="precise-rate"></div>
