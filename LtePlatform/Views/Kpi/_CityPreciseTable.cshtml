<h4 class="text-center text-primary">
    分区指标统计
</h4>

<table class="table table-striped">
    <thead>
    <tr>
        <th>城市</th>
        <th>区</th>
        <th>MR总数</th>
        <th>精确覆盖率</th>
        <th>第一邻区精确覆盖率</th>
        <th></th>
    </tr>
    </thead>
    <tbody data-bind="foreach: districtStats">
    <tr>
        <td data-bind="text: $data.city"></td>
        <td data-bind="text: $data.district"></td>
        <td data-bind="text: $data.totalMrs"></td>
        <td data-bind="text: $data.preciseRate.toFixed(2)"></td>
        <td data-bind="text: $data.firstRate.toFixed(2)"></td>
        <td><button class="btn btn-primary" data-bind="click: function(){ $root.showDetails($data.district);}">详细</button></td>
    </tr>
    </tbody>
</table>

<h4 class="text-primary text-center">
    <span data-bind="text: currentDistrict"></span>详细统计信息
</h4>

<table class="table table-hover">
    <thead>
    <tr>
        <th>镇区</th>
        <th>MR总数</th>
        <th>精确覆盖率</th>
        <th>第一邻区精确覆盖率</th>
        <th>第三邻区精确覆盖率</th>
    </tr>
    </thead>
    <tbody data-bind="foreach: townStats">
    <tr>
        <td data-bind="text: $data.town, visible: $data.district == $root.currentDistrict()"></td>
        <td data-bind="text: $data.totalMrs, visible: $data.district == $root.currentDistrict()"></td>
        <td data-bind="text: $data.preciseRate.toFixed(2), visible: $data.district == $root.currentDistrict()"></td>
        <td data-bind="text: $data.firstRate.toFixed(2), visible: $data.district == $root.currentDistrict()"></td>
        <td data-bind="text: $data.thirdRate.toFixed(2), visible: $data.district == $root.currentDistrict()"></td>
    </tr>
    </tbody>
</table>