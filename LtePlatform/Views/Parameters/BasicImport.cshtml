@{
    ViewBag.Title = "基本工程参数信息导入";
}

<div class="panel panel-warning">
    <div class="panel-heading">
        <h2 class="panel-title">
            <strong>@ViewBag.Title</strong>
        </h2>
    </div>
    <p>
        @{
            Html.RenderPartial("../Kpi/_KpiMenu");
        }
    </p>
</div>

<div class="row" ng-app="myApp">
    <div class="col-md-4">
        <h4>工参文件选择</h4>
        @using (Html.BeginForm("LteImportPost", "Parameters",
                FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal", role = "form" }))
        {
            <p><label class="control-label">LTE Excel文件选择：</label></p>
            <div class="form-group">
                <div class="col-md-8">
                    <input name="lteExcel" class="form-control" type="file" accept=".xlsx"/>
                </div>
                <div class="col-md-4">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-save"></span>保存
                    </button>
                </div>
            </div>
        }
        @using (Html.BeginForm("CdmaImportPost", "Parameters",
            FormMethod.Post, new {enctype = "multipart/form-data", @class = "form-horizontal", role = "form"}))
        {
            <p><label class="control-label">CDMA Excel文件选择：</label></p>
            <div class="form-group">
                <div class="col-md-8">
                    <input name="cdmaExcel" class="form-control" type="file" accept=".xls"/>
                </div>
                <div class="col-md-4">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-save"></span>保存
                    </button>
                </div>
            </div>

        }
    </div>
    <div ng-view></div>
</div>

@section Scripts{
    <script src="~/Scripts/angular.js"></script>
    <script src="~/Scripts/angular-route.min.js"></script>
    @Scripts.Render("~/bundles/ui-bootstraps")
    @Scripts.Render("~/bundles/highcharts")
    <script src="~/Scripts/highcharts/highcharts-ng.js"></script>
    <script src="~/Scripts/service/app.url.service.js"></script>
    <script src="~/Scripts/service/app.region.service.js"></script>
    <script src="~/Scripts/service/app.kpi.service.js"></script>
    <script src="~/Scripts/service/app.parameters.service.js"></script>
    <script>
        var app = angular.module('myApp',
            ['ngRoute', "highcharts-ng", "ui.bootstrap", 'myApp.url', 'myApp.region', 'myApp.kpi', 'myApp.parameters']);
    </script>
}